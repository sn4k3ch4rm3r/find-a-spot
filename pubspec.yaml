name: find_a_spot
description: "An application to share and find great spots."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  dynamic_color: ^1.7.0
  flutter:
    sdk: flutter
  flutter_localization: ^0.2.0
  intl: ^0.19.0
  json_annotation: ^4.9.0
  http: ^1.2.1
  provider: ^6.1.2
  firebase_core: ^2.31.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: ^2.4.10
  json_serializable: ^6.8.0

flutter:
  uses-material-design: true
  generate: true